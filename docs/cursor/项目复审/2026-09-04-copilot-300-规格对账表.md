# 规格对账表（W1）：webnovel-copilot-300 07 功能流 → 08 实施计划

> 用途：补做 v8.0.0 发版前缺失的 F→T 反向对账（复审报告 P0-4）。左列抄 `docs/zcode/webnovel-copilot-300/07-feature-flows.md` 标题原文，右列为 08 计划中的 T 任务及其 commit 证据；「部分」列引用 `docs/zcode/v8-gap-review-3rounds/README.md` 的缺口编号。
> 规则：任何 F 在右列为空即为「漏了」；本表建立时 F-13 即为此状态。
> 生成方式：正则抓取 07 `^## F-\d+` 标题与 08 全文 `F-\d+` 引用做差集（`['F-01','F-13']`），再逐 T 人工归因。

## 对账：M0-M7（T1-T34）对 F-01～F-14

| # | 方案条目原文（出处：07 §） | 状态 | 证据（T 任务 / commit / 缺口） |
|---|---|---|---|
| 1 | 「F-01 · author-sync（修改触发记录 · 主入口）」 | ✅实现（08 未标 F-ID） | T2 journal 数据面 `c2e961e`；T3 author-sync 脚本分类 `dae7fa6`；T4 语义补全与影响摘要 `d3102ad`；T5 impact 引用反查；SessionStart hook 调 `author-sync`（`hooks/session_start.py`） |
| 2 | 「F-02 · 总纲分阶段管理与 regen 画廊」 | ✅实现 | T6 总纲三区 `e8ee13b`；T7 regen 画廊 `78a8a16`（08 计划写的 `8f0a5c9` 在本仓不存在，见勘误） |
| 3 | 「F-03 · 卷纲时间线视图」 | ✅实现 | T9 `0686cb2`；年龄推演按计划移至 T29 `63d99eb` |
| 4 | 「F-04 · 章纲批量生成与单章 regen」 | 🔶部分 | T8 `970ff4a`（批量 + 4 项自检）；T7 单章 regen。缺口：v8-gap-review B1-B5（自检缺账本存在性 / 跨批时间锚单调 / 境界链 / 名册校验 / 详细大纲标题对照）、N1/N3 详细大纲路径三分裂 → 修复计划 P2-1 / P2-2 |
| 5 | 「F-05 · 素材流转（活层 → 定版 → 轨迹）」 | 🔶部分 | T11 `a56bda2`；T12 `6b0fbc1`（v6 chapter-commit 投影后自动落账）；T13 `02e1d52`。缺口：v7 `settle` 零后置钩子，`materials log` 在 v7 路径需手动（E 类 / N10）→ P3-1 |
| 6 | 「F-06 · material-review（素材卷审）」 | ✅实现 | T14 `4c4e92b`（+`ef47c7d`） |
| 7 | 「F-07 · 定版修改与 retcon（冻结变更裁决）」 | ✅实现 | T10 freeze/retcon（retcon 三选项，受影响章 35/38 实测） |
| 8 | 「F-08 · 设定工坊（四生成器，提案模式）」 | 🔶部分 | T20 `1695242`（prepare/save/adopt/confirm + 红线程序化）。缺口：N5 `confirm` 写入的 `power_anchor_sync:required` / `contract_rebuild:required` 标记无执行器消费 → P3-2 |
| 9 | 「F-09 · 战力校验（power-check）」 | ✅实现 | T18 锚点抽取 `cc18f66`；T19 战例账本与 power_check `cc18f66`（硬①②阻断 / 软③非阻断） |
| 10 | 「F-10 · 卷收尾冻结（freeze）」 | ✅实现 | T10；T30 volume-reconcile 与 freeze 联动 `fb1f20d` |
| 11 | 「F-11 · 写章链（v7 主链 + 增强点标注）」 | 🔶部分 | T22 连续性包 `8d95bf6`；T23 prose_check + 第 6 维 `bb01308`；T24 多稿 `83907be`；T25 反馈闭合 `b439d50`；T26 `0046dd7`；T27 `3dde421`。缺口：v7 `build_context_pack` 仅 7 section（C1-C14：stale_notes / author_model / style_anchor / pending_promises 等未接入 v7 路径，T22/T25 的 section 只进了 v6 `context_manager`）；v7 `settle` 不读 `review_results.json`、无 prose_check 门禁（D1-D3 / N10）；决策卡 pov 无消费者（N11）→ P1-1 / P1-2 |
| 12 | 「F-12 · 学习闭环（被动为主）」 | ✅实现（CLI 瑕疵） | T16 `4c6be51`（learn --from-journal / apply / 双层回写）；T17 style_anchor `729a505`。瑕疵：N9 `learn learn --from-journal` 位置参数冗余 → P3-3 |
| 13 | 「F-13 · doctor 治理层体检」<br>「新增检查组：journal 水位 vs git 状态（未留账告警）、stale 积压、定版-轨迹一致性、锚点-正文对账、条目状态机、素材健康摘要、画廊积压（>2 卷未清理）、合同编译一致性（重建对账）。」 | **❌漏了** | 08 计划 T1-T34 无任一任务引用或实现；`doctor.py` grep `journal\|stale\|ledger\|backlog\|promise` 0 命中（复审报告 §6）。**未排期，非实现失误**。处置：转 v8-gap-review 修复计划 P4-1；且 P4-1 前置「v7 书仓项目根解析」（复审 P1-9：doctor 在纯 v7 书仓解析不到根，12 组既有检查整体跳过） |
| 14 | 「F-14 · 素材工作台（会话模式 + dashboard 视图）」 | 🔶部分 | T31 `/webnovel:materials` 命令 `56ac680`；T32 dashboard 六视图 `2c35af0`。缺口：N6 governance.py 不读承诺账本（条目 / 逾期视图缺）→ P4-2 |

**汇总**：14 项 F 中 ✅ 8、🔶 5、❌ 1。08 计划的 34/34 `[x]` 对 T 层成立，对 F 层不成立——「T1-T34 全队列完毕」≠「07 方案全覆盖」。

## 漏了怎么办

- F-13：不回补进 08 计划（该计划已封版发版），改在 `v8-gap-review-3rounds` P4-1 落地；08 计划顶部已加「不覆盖 F-13」显式声明（本次同步修改）。
- 🔶 五项：均已归入 v8-gap-review 4 阶段修复计划，本表只做归因，不重复排期。

## 08 计划证据行勘误（W3）

| T | 原证据行 | 事实 | 勘误 |
|---|---|---|---|
| T34 | 「版本 7.1.0→8.0.0 三处同步（plugin.json/marketplace 双位置/**mcp SERVER_VERSION**/README 徽章与版本表）」 | `git show --stat cff0ef0` 只改 `plugin.json` / `reference-loading-map.md` / `validate_reference_wiring.py`；`mcp/server.py` 最后改动是 T31 `56ac680`，`SERVER_VERSION` 停在 7.1.0 直到 2026-09-04 | SERVER_VERSION 实际在 `1cc23ce` 同步，并把 `server.py` 纳入 `sync_plugin_version.py` 校验集合（此前 `--check` 假阴性） |
| T7 | 「证据 8f0a5c9（见 git log，提交信息含 T7）」 | `git cat-file -e 8f0a5c9^{commit}` 失败；`git log --all --oneline | grep T7` 唯一命中 `78a8a16 feat: T7 regen 画廊…` | 证据 hash 改为 `78a8a16`（本表对 T1-T34 引用的全部 29 个 hash 做了 `cat-file -e` 存在性校验，仅此一处不存在） |
| T34 | 「README/CHANGELOG/releases/v8.0.0.md + **guides 更新**」 | `docs/guides/commands.md` 在 `d726307` 之前零覆盖 v8 命令面（`/webnovel:` 0 次、MCP 0 次、11 个治理子命令缺 10 个） | guides 实际在 `d726307` 更新 |

## 自查口令

「这个任务的验收标准，是方案说了算，还是我写的测试说了算？」——T34 的两条证据行都是后者：测试与校验器全绿，但方案要求的对象（server.py 版本、guides）没被碰到。校验器只能查它被告知要查的东西。
