# 项目全面审阅报告（2026-09-02）

> 审阅对象：webnovel-writer 全项目（v6 运行时 + v7 story-repo 新架构，`v7-tmp` @ `0bcf895`）
> 方法：两个只读探查子代理（代码面盘点 / 规格与历史问题核对）+ 关键发现主代理亲自复核 + **ZCode 实测跑第 38 章**（v7 全链路）+ 全量测试与覆盖率测量
> 关联：[S20 切片复盘](2026-09-02-S20-v7垂直切片复盘.md)、[S22 配额分析](2026-09-02-S22-配额确定时机分析.md)、[2026-08-24 两份分析](../full-project-analysis-v2-2026-08-24.md)（本报告核对其遗留项状态）

## 1. 总评

**项目处于「v7 主轴功能闭环、发版前」的健康状态**：写路径最小闭环已被两章实测验证，护栏体系（机检、唯一写入路径、原子 settle）在真实数据上按设计工作；测试面 1062 用例全绿 + 22/22 行为评测，限定范围覆盖率 89%；技术债标记极少（全仓仅 2 处 TODO/FIXME）。代码规模 Python 约 34,500 行 / 115 个模块，结构清晰无失控巨型文件。

审阅发现 **1 个潜伏 P1**（名册双落点：v7 原生新书的实体查询面是死的）、**1 个功能缺口**（承诺系统在 v7 链路缺位）、以及一批 P2/P3（规格回写、覆盖率门槛失真、历史遗留对账缺口）。均不阻塞 S21 发版，但建议 v7.0.0 前修掉 P1 与覆盖率门槛两项。

## 2. 实测：第 38 章《回纸》v7 链路全流程（ZCode 实跑）

### 2.1 流程与结果

| 步骤 | 结果 |
|---|---|
| 决策卡 | `工作区/决策卡-0038.md` 生成（37 字段口径：目标 2,678/下限 2,008、4 节拍、4 禁区、4 合同断言、豁免勾选） |
| 上下文包 | 3,242/20,000 字符（**16.2%**，`sections_before` 2,869）；7 关键实体全部经 v7_cache 解析命中 |
| **S23 新信号（真数据首用）** | `truncated_sections=['prev_chapter_tail']`、`budget_used_ratio=0.162`——S22 预测的触顶节被自动捕获，按书校准的价值实证（重迁移预填 1,339 可解） |
| 草稿 | 首写 1,642 字（61% 目标，**与 S19 首稿同一坑**）→ 两轮扩写至 2,386 字（89% 目标） |
| 机检 | **通过**（`ok=true`，2,386 ≥ 2,008；无占位符；标题一致；承诺豁免有效） |
| 名册 advisory | 报「空位」「风暴」两个引号词**假阳性**（非人名；启发式局限，作者门处置） |
| settle | **已完成（作者验收通过）**：原子 commit `a4def9c`（`settle: 第0038章 回纸`，正文 0038-回纸.md 2,377 字 + 章摘要），S18 唯一写入路径对真书校验通过；缓存重建 38 章可查、verify 等价 |

### 2.2 实测观察

1. **护栏有效且肯拦自己人**：字数下限（2,008）对着目标 89% 的成稿留有真实约束力；S19 的「机检先于模型评审」设计在第二轮实测中继续成立。
2. **字数契约的拉力仍靠写手自觉**：首写两次低于目标（61%→81%→89%），机检只拦下限不推上限；上下文包的 `length_contract` 节是提示而非硬闸。观察项，非缺陷。
3. **计量口径**：本轮消耗在下一轮钩子统计可见（本会话前 3 轮：总 1,415 万/去缓存新增 18.2 万）；S20 复盘的「第 38 章独立新会话干净计量」条件本次不满足（同会话执行），M3 的「有条件达标」维持。

### 2.3 第 38 章验收卡（作者已验收，2026-09-02）

- **章名**：《回纸》（第 0038 章，视角 苏小白，末世第19天 清晨）
- **剧情**：夜里西挪绊索实动 → 清晨回纸上钩（写的正是东墙假信内容，泄密渠道锁定东墙信源）→ 四人名单进暗账不收网 → 风暴进入五天倒计时 → 钩子：城北把「安稳价码」写成「带粮来，换命」钉上篱笆
- **合同断言核验**：西挪实动✓ / 回纸验证东墙信源✓ / 暗账名单未惊动✓ / 倒计时五天✓ / 禁区（城北正面戏、熊铁山出场、提前收网、风暴到来）均未触碰
- **字数**：2,386（目标 89%，下限余量 +378）
- **名册 advisory**：「空位」「风暴」为引号词假阳性，建议确认无需入册
- **settle 动作**：正文 `0038-回纸.md` + 章摘要 + git commit（含 S18 唯一写入路径对真书校验）；无新实体入册

## 3. 代码面盘点（探查代理数据）

- **规模**：`scripts/` 33 文件 10,945 行 + `data_modules/` 74 文件 23,509 行 + memory/ 8 文件 + write_gates/ 4 文件；>1500 行文件仅 2 个（rag_adapter 1,625、state_manager 1,510）。
- **CLI 面**：48 个命令解析器（36 顶层 + 12 嵌套），v6 全功能 + v7 三脚本（migrate/v7_cache/v7_write）暂在 `scripts/` 层独立入口。
- **技术债信号**：TODO/FIXME 全仓仅 2 处（且非债务标记）；dead code 候选 9 个（user_report 4 个 build_*_report、cli_output 4 个、settings_digest.digest_dir），广谱扫描其余 445 个顶层定义均有引用。
- **结构观察**：v6/v7 双链并行由 dual_format_guard 收口；state_manager(JSON) 与 sql_state_manager(SQLite) 并存（后者仅迁移器引用）；config.py 单类 **139 字段**；11 个文件带 UTF-8 BOM；data_modules 约 40% 模块无 docstring。

## 4. 质量面

- **测试**：108 个测试文件 / 980 个测试函数 → 收集 **1,062 用例全绿**（S23 后口径）；22/22 行为评测。
- **覆盖率（本次勘误）**：pytest.ini 的 `--cov` 未限定范围，裸跑测得 19.31%（把非测试触达文件全算进去）**并触发 90% 门槛误报**——工作契约里 `--no-cov` 的写法正是被这个失真逼出来的绕过。限定 `--cov=scripts/data_modules + scripts/*.py` 后实测 **89%**（12,485 语句 / 1,417 未覆盖），门槛名实基本相符。
- **规格与指南**：guides/commands 与 8 个 SKILL、operations.md 命令面全部与代码一致、无脱节；**缺口是 v7 三脚本无任何使用指南**（commands.md 只覆盖 v6 链路）。

## 5. 规格一致性（story-repo-spec 0.4 对照，主代理复核后口径）

| 契约点 | 判定 | 说明 |
|---|---|---|
| 正文命名/front matter/UTF-8/git 卫生/迁移映射 | 符合 | S20 已核销三项命名漂移 |
| 缓存「派生物可丢弃」与重建验收 | 符合 | verify 快照等价在库 |
| **名册落点** | **漂移（P1）** | 见 §7 |
| 承诺系统 | 缺口（P1-功能） | spec §5 结转规则未实现：机检只验「承诺非空或豁免」，无承诺档案读写；切片章以豁免通过 |
| commit message `ch(NNN):` 格式 / front matter 承诺键 / 决策卡固定四段 | 漂移（P2） | 实现为 `settle: 第NNNN章`、键为「承诺豁免/合同」、决策卡为实测演化形态 |
| book.yaml 平铺禁嵌套（§2.2） | 漂移（P2） | S23 的 `context_budget:` 引入两级嵌套——**规格与实现的收敛方向需要作者裁决**：修 spec（0.5）承认实测形态，或改实现 |
| 缓存表 promises/secrets/fingerprints | 未建（P2） | v7_cache docstring 自述 E2 最小面；依赖承诺系统整体排期 |

## 6. 历史问题核销状态（2026-08-24 分析 + pending-work 清单）

- **已修复**（抽验证实）：SQLite WAL/busy_timeout（db.py 统一工厂）、timeline_check 接入 prewrite、守卫正则失效、writing/ 空壳清理。
- **仍存在**（复核属实，v6 侧）：H2 total_words 三路写入无对账（P2）；H3 大纲裸标签正则不容 markdown 变体（P3）；M5 裸 int() 防御不一致（P3）；M7 file_signature 对换行/BOM 敏感（P3）；M16 SKILL 裸 `python` 残留 3 处（P3）。
- **pending-work 7 条未勾项**：4 条已完成可核销（迁移器/缓存重建/垂直切片/唯一写路径）、1 条部分完成（题材 taxonomy 缺入口测试）、1 条 superseded、1 条确实未做（多宿主——S20 已立项排 S21 后）。

## 7. 风险清单（按优先级）

> **修复状态更新（S24，2026-09-02 同日执行，Human 指示「按优先级修复」）**：P1 名册双落点 ✅（v7_cache 兼读双落点+first_chapter，真仓重建验证）；P2 覆盖率门槛 ✅（.coveragerc 钉范围、门槛 80，实测 81.59%）；P2 spec 漂移 ✅（spec 0.5 变更草案六项显式登记，待作者批准）；P2 settle 回滚 index ✅；P3 v7 使用指南 ✅ / M16 裸 python ✅ / taxonomy 入口测试 ✅。**延后**：承诺系统（v7.1）、H2 对账（v7 迁移后消亡）、死代码删除（探查清单不可靠需逐符号核验——cli_output 四定义实为传递使用）、BOM/docstring 卫生批。

| 级 | 发现 | 证据 | 影响 |
|---|---|---|---|
| **P1** | **名册双落点**：settle 写 `定稿/设定/名册/<正名>.md` 目录，迁移器与 v7_cache 只读 `名册.md` 单表 | v7_cache.py:91-101 vs v7_write.py:352-356；两文件在现仓并存 | 现仓因迁移表恰好已含相关实体而未爆发；**v7 原生新书（不经迁移）实体缓存面=只有主角保底**，新实体永远进不了查询面（find_entity→「名册未登记」）。修法二选一：v7_cache 兼读目录，或 settle 改写单表——建议前者（目录形态是实测演化方向） |
| **P1-功能** | 承诺系统 v7 缺位（见 §5） | run_checks 仅 waiver 检查 | v7 链路无法承载「承诺结转」这一长线质量机制；v7.0.0 可带缺口发版，但 spec 0.5 应显式标注 |
| P2 | spec 0.4 与实现的三处格式漂移 + book.yaml 嵌套裁决（见 §5） | — | 不修则每章 settle 产物与 spec 持续互相矛盾 |
| P2 | 覆盖率门槛失真：pytest.ini `--cov` 无范围 | 裸跑 19.31% 误报 vs scoped 89% | 门槛逼出 `--no-cov` 绕过，失去守门意义；修法：addopts 钉 `--cov=webnovel-writer/scripts/data_modules` |
| P2 | H2 total_words 三路写入无对账（v6 遗留） | state_manager.py:282/663、state_projection_writer.py:98 | 真书长期运行的字数统计漂移风险，v7 迁移后自然消亡 |
| P2 | settle 回滚不 reset git index | v7_write.py:361-374 | commit 失败时 index 残留 staged 条目（下次 add 自愈），P3 边缘 |
| P3 | dead code 9 处 / BOM 11 文件 / 40% 无 docstring / genre_taxonomy 缺入口测试 / v7 无使用指南 / S13a 模型路由挂起 | 探查代理清单 | 卫生项，随 S21 发版或专清理批处理 |

## 8. 结论与排队建议（不动队列，供作者裁决）

1. **实测结论**：v7 链路两章两轮实测均闭环（第 38 章 settle `a4def9c`），护栏真实有效；第 38/37 章作者均已验收。
2. **建议 v7.0.0（S21）前插入一个小修复批**：P1 名册双落点（v7_cache 兼读目录 + 测试）+ 覆盖率门槛钉范围 + v7 三命令使用指南一页——合计约 0.5-1d，作为 S21 的前置或并入 S21。
3. **spec 0.5 回写**（承诺系统排期时一并做）：承认实测形态（settle 命名/决策卡结构/名册目录/book.yaml context_budget 嵌套），承诺系统与缓存 promises 表列为 v7.1 范围。
4. **S21 发版本身仍待作者确认版本号与 push**（既有约定不变）。
