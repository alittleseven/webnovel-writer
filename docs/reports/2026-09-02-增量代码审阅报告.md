# 增量代码审阅报告（2026-09-02）

> 审阅对象：`@7e3bfe2`（S24 修复批 + spec 0.5 批准之后的最新代码）
> 方法：4 个只读探查子代理分域通读（v7 写路径 / v6 状态投影 / CLI 与上下文 / dashboard·hooks·发布链）+ 主代理对全部高危发现逐条亲读源码复核 + 全量测试取证
> 关联：[项目全面审阅报告](2026-09-02-项目全面审阅报告.md)（同日早些时候，本报告是其**增量**：核验 S24 修复质量 + 独立复查新发现）；流程与数据流交付见 [functional-flow-and-data-flow](../architecture/functional-flow-and-data-flow.md)

## 1. 总评

- **测试面**：1071 用例全绿（69s），限定覆盖率 **81.65% ≥ 80 门槛**——S24 的覆盖率修复实测有效。
- **S24 修复核验**：五项修复主路径全部落地且实现正确（见 §2），但其中两项存在残留缺口（§3 P2-5 / P2-2），一项（裸 python）只修了一半。
- **新发现**：本轮独立复查发现 **2 个建议 P1 的新问题**（迁移器名册列序错位、v7 连续写作缓存陈旧），以及一批 P2/P3。集中模式很清晰：**v7 垂直切片的最小面是健壮的，切片边界之外（迁移器、缓存生命周期、防双写强度、默认关闭的守卫）质量明显陡降**；v6 侧新发现则以「静默竞写/状态机绕过」为主。
- **修复排队建议**：P1 两项 + extract-context 默认参数，合计约 0.5d，建议并入 S21 发版前修复批；其余按 §4 排队。

## 2. S24 修复批核验（主代理亲验）

| 修复项 | 判定 | 核验证据 |
|---|---|---|
| P1 名册双落点兼读 | ✅ 正确 | `_iter_roster` 先单表后目录、同名目录覆盖；`test_directory_overrides_single_table_on_overlap` / `test_v7_native_book_directory_only` 两形态有测试 |
| P2 settle 回滚清 git index | ✅ 主路径 / ⚠️ 有缺口 | 正常失败路径：unlink created + `git reset --quiet`，无中间态；但见 §3 P2-5 两个缺口，且该分支零测试（覆盖率佐证：`v7_write.py:375-378` 未覆盖） |
| P2 覆盖率门槛钉范围 | ✅ 有效 | `.coveragerc` + `pytest.ini` 实测 81.65% 过门槛 |
| P2 spec 0.5 回写 | ✅ 生效 | 作者已批准（`7e3bfe2`），六项偏差显式登记 |
| P3 v7 使用指南 | ✅ / ⚠️ 一处表述失实 | `docs/guides/v7-write-path.md` 已发布；但「首查自动触发 [rebuild]」与实现不符（见 §3 P1-2） |
| M16 SKILL 裸 python | ⚠️ 部分修复 | S24 只修了 init/dashboard 主文件；plan/query/review 主文件仍余 6 处 + query 参考 9 处（§3 P3-14） |
| P3 genre_taxonomy 入口测试 | ✅ | `028e4da` |

## 3. 新发现问题清单

> 标注「✓亲验」= 主代理已亲读源码逐行确认；「代理报告」= 探查代理给出、逻辑自洽但未逐行复核。

### P1 — 建议 S21 发版前修复

> **修复状态更新（P1 修复批 / 队列 S25，2026-09-02 同日执行，Human 指示「P1-3 都处理」）**：P1-1 ✅（migrate 列序对调；修复中另发现同族缺陷——`v7_cache` 单表解析器硬编码 `first_chapter=""` 不读第三列，一并修复并保持两列旧表兼容）；P1-2 ✅（settle 尾迹自动 rebuild，best-effort 不回滚事务，结果透出 `cache_rebuilt`；`_conn` 增四表健康检查，零字节/坏库/缺表首查自动重建）；P1-3 ✅（包装层 `--format` 对齐底层 json-only，`choices/default` 均改 `"json"`）。新增 7 条回归测试（Red→Green，含迁移名册查询面断言），全量 1078 绿 / 覆盖率 81.69% / 行为评测 22/22。P2/P3 未动，维持 §4 排队。

**P1-1 迁移器名册列序错位（别名列与首现章列互换）** ✓亲验
- 证据：`scripts/migrate_v6_to_v7.py:158-165` SQL 返回序为 `(canonical_name, first_appearance, aliases)`，元组按此序存入；`:177` 消费端 `for name, aliases, first in roster_rows` 按 `(名, 别名, 首现章)` 解包 → **名册.md 的别名列填的是首现章、首现章列填的是别名串**。
- 边界讽刺：`:174` 主角保底行 `(name, "", "1")` 顺序恰好正确，掩盖了错位；测试无「index.db 含别名」用例故漏网。
- 影响：所有经 index.db 迁移的名册行两列互换；连带污染 v7_cache 单表读取的 `aliases`/`first_chapter`（`find_entity` 别名匹配失效）——S24 刚修好的双落点读取面，其单表数据源本身是错的。
- 修法：`:159` 元组改为按 `(r[0], r[2] or "", str(r[1] or ""))` 组装（或 SQL 调序），补一条带别名的迁移测试。

**P1-2 v7 连续写作缓存陈旧：settle 不刷新、损坏不自愈** ✓亲验
- 证据：settle 成功后不触发 `rebuild_cache`；`v7_cache.py:198-202` `_conn` 仅在**文件不存在**时重建。
- 影响：连续写第 N+1 章时 `pack` 的 `get_summary(N)` 查不到刚 settle 的摘要、`find_entity` 查不到新实体——上下文静默降级，直到有人手动 rebuild。指南「首查自动触发」表述失实。
- 连锁：库文件存在但损坏（零字节/半写坏，无表）时所有查询抛 `no such table` 且不触发重建，须人工删 `.cache/`——违背 spec §1.2「派生物可丢弃、首查重建」不变量。
- 修法：settle 成功尾迹调用 `rebuild_cache`；`_conn` 改为「文件缺失 **或** 健康检查失败（如 `SELECT 1 FROM meta` 失败）」时重建。

### P2 — 建议尽快处理

**P2-1 settle 防双写只查精确文件名** ✓亲验
- `v7_write.py:309-311` 只检查 `NNNN-标题.md` 精确路径存在；同章**改标题**即可二度 settle → 定稿出现两个同章号文件 → `rebuild_cache` 因 `chapters.num` 主键 + 裸 INSERT（`v7_cache.py:164-175`）抛 IntegrityError，缓存永久不可用（叠加 P1-2 的「存在即不重建」）。
- 修法：按章号前缀 `glob(f"{chapter:04d}-*.md")` 判重（与 `dual_format_guard.has_v7_settled_chapter` 的前缀扫描对齐）。

**P2-2 settle 回滚的两个缺口（S24 修复的补丁）** ✓亲验
- (a) `v7_write.py:371-373` unlink 循环未逐个 try：Windows 下文件被 AV/编辑器锁定时 unlink 抛异常会在 except 块内直接传播、**跳过 `git reset`**——留下 add 进 index 的半成品条目，恰是 S24 要消灭的中间态。
- (b) `git reset --quiet` 清的是整个 index：会把作者此前自行 staged 的无关改动一并 unstage（副作用未声明）。
- 该分支无测试（`test_v7_write.py:192-208` 用 `commit=False`，`git reset` 路径零覆盖）。

**P2-3 settle 的 git commit 无身份兜底且报错不可读** ✓亲验
- `v7_write.py:369` `_git(... "commit" ...)` 未带 `-c user.name/-c user.email`（对比迁移器 `_git_init` 有）；`_git`（:280-281）`capture_output=True` 吞掉 stderr → 无全局 git identity 的环境 settle 必败 → 回滚删章，且错误只剩 returncode。建议身份兜底 + CalledProcessError 时透出 `e.stderr`。

**P2-4 双格式守卫默认不生效** ✓亲验
- `config.py:220` `story_repo_root` 仅从 env `STORY_REPO_ROOT` 读取、默认空；迁移器 docstring 声称「建立映射」但**实际不写任何映射**（全仓 grep 仅 config 与测试引用）。v6 侧守卫只挂 prewrite 一道（precommit/postcommit 无）；v7 侧靠 decision 临时键 `v6_project_root`（`v7_write.py:304`）。两端配置默认都关——「唯一写入路径」防护实际需要人工接线。
- 修法：迁移器落一份映射（如书仓 `.webnovel/story_repo_root` 或 env 提示），precommit 补挂守卫。

**P2-5 v7_cache 对损坏输入零容错** 代理报告（与 P1-2 合并修复即可）
- `_iter_roster_directory`（`v7_cache.py:121-136`）对名册目录文件读取无异常保护：单个非 UTF-8 文件让整个 `rebuild_cache` 崩溃，单文件损坏放大为全查询面不可用。违背 spec §10 第 0 态「永不带堆栈崩溃」。

**P2-6 dashboard 无鉴权 + 数据库读写模式连接** ✓亲验
- `dashboard/server.py:46` 默认 `127.0.0.1` 尚可，但 `--host 0.0.0.0` 一个参数即把全书正文/state/合同暴露到局域网（无任何 Token）。CORS 只防浏览器跨域，不防直连。
- `app.py:314-320` `_get_db` 以默认**读写**模式连 index.db（无 `mode=ro` URI、无 busy_timeout）：违背「严格只读」定位，可能生成 -wal/-shm，写并发下 `database is locked` 直接 500。
- 修法：连接串加 `file:...?mode=ro`；绑定非回环地址时要求随机 Token（或拒绝并打印指引）。

**P2-7 v6 rejected 覆写绕过单调状态机** ✓亲验
- `state_projection_writer.py:59-65`：commit status=rejected 时**无条件**把 `chapter_status` 覆写为 `chapter_rejected`——已 committed 的章被 replay 成 rejected 时状态回退，绕过 `state_manager.py:307-313` 的单调保护。

**P2-8 H2 total_words 三路写入仍在（已知延后项，本轮补证据）** 代理报告 + grep 亲验
- W1 `state_manager.py:280-285`（锁内合并）、W2 `:662-664`（内存累加无锁落盘）、W3 `state_projection_writer.py:98-102`（投影重算覆盖），三路口径不同、无对账。
- 新证据：`word_counted_chapters` 增量缓存**只读不写**（`state_projection_writer.py:459` 为全仓唯一引用）→ 缓存分支是死代码，每次全量重读各章正文计数。
- 维持全面审阅的处置（v7 迁移后消亡）；若 v6 还要长跑，建议先收敛为单一写入器 + doctor 加 `SUM(chapters.word_count)` 对账。

**P2-9 决策卡不写 promises → 承诺机检对标准流程形同虚设** 代理报告
- `write_decision_card`（`v7_write.py:55-75`）不落 `promises` 字段，`run_checks` 的 `promise_ok = promises or waiver` 对走标准决策卡的章恒依赖 waiver。属已登记 v7.1 承诺系统缺口的子症状，spec 0.5 第 6 条已显式标注——列在此处仅为提醒：**豁免泛滥时机检的承诺项等于没有**。

### P3 — 卫生与文档

| # | 发现 | 证据 |
|---|---|---|
| 14 | SKILL 裸 python 残留：主文件 6 处（plan:36/208/217、query:20、review:30/111）+ query 参考 `system-data-flow.md` 9 处 + init:185（`--help`，良性） | ✓亲验 grep；S24 只修了 init/dashboard |
| 15 | 文档-代码漂移：`agents/reviewer.md:30` `get-state-changes --limit 20` 缺必填 `--entity`；query SKILL `query-rules --chapter` 参数不存在（`memory_cli.py:90-91` 只有 `--domain`）；chapter-commit 包装层参数「可选」但底层全必填（`webnovel.py:825-828/933-940` vs `chapter_commit.py:36-39`） | 前两项 ✓亲验 |
| 16 | **extract-context 默认参数必败**：`data_modules/webnovel.py:813` 声明 `--format choices=[text,json] default="text"`，但底层 `extract_chapter_context.py:383` 仅收 `choices=["json"]` → 不带 `--format json` 调用直接 exit 2 | ✓亲验（升级为 P1-3 见下） |
| 17 | guard_runtime_write Bash 拦截名单漏 `cp/mv/tee/sed -i/dd/git checkout --`（`guard_runtime_write.py:108`）；deny 走 stderr+exit2 而非 stdout JSON | 代理报告 |
| 18 | watcher 与 WAL 不匹配：`index.db` mtime 在 checkpoint 前不变 → SSE 实体变更迟滞（`watcher.py:31/49`）；SSE QueueFull 静默退订、无心跳无重连 | 代理报告 |
| 19 | backup 回滚为前滚式 checkout，不删除 tag 之后新增文件，与「100% 一致」docstring 不符（`backup_manager.py:350`） | 代理报告 |
| 20 | `run_ledger.py:74-78` write_text 非原子无锁，读-改-写并发可丢更新 | 代理报告 |
| 21 | state_changes 去重为 check-then-insert、表无唯一约束，replay/并发可插重复行（`index_projection_writer.py:163,248-273`） | 代理报告 |
| 22 | 性能：memory store 每条 upsert 全量 load+save（一章 N 条 O(N²)）；IndexManager 每操作新开连接；v7 pack 的 `book_word_stats` 每章全量重读全书正文；dashboard `/api/commits` 每请求全量 parse、多个接口无 LIMIT 全表倾倒 | 代理报告 |

> **P1-3（从 P3 升级）**：上表 #16 的 extract-context 默认参数问题虽属文档-代码漂移，但它让「按帮助信息使用命令」的默认路径必败（exit 2），且 context-agent 的降级路径正依赖此命令——按影响面归 P1 级接口缺陷，修法一行（包装层 `default="json"` 或底层支持 text）。

## 4. 结论与修复排队建议（不动队列，供作者裁决）

1. **S21 发版前小修复批（约 0.5-1d）**：~~P1-1 名册列序、P1-2 缓存刷新/自愈、P1-3 extract-context 默认值~~（✅ 已由 S25 修复批完成）；P2-1 章号判重（一行 glob）、P2-2/P2-3 settle 回滚与 git 身份兜底（同文件顺手）——仍待做。
2. **随 v7.1**：P2-4 守卫接线（依赖迁移器补映射的裁决）、P2-9 承诺系统（已登记）。
3. **独立小批**：P2-6 dashboard 只读连接 + Token；P2-7 rejected 状态机；P3-14/15 文档漂移批量修。
4. **维持延后**：H2/P2-8（v7 迁移后消亡）、性能项（千章书出现痛点再做）。
5. 流程图与数据流全景（本次任务交付物）见 [functional-flow-and-data-flow](../architecture/functional-flow-and-data-flow.md)，其中数据落点表已把「真源/投影/派生」逐项标注，可作上述修复的对账底册。
